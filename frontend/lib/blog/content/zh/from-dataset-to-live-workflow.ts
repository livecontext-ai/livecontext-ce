// from-dataset-to-live-workflow - zh
const content = `一个数据集在有某件事因它而发生的那一刻，才变得有用。在那之前它只是一个文件。下面是我们用来把一个静态的利基数据源，变成一个自主运行、并以真实动作收尾的工作流的那套形状。

为了保持具体，有一个例子会贯穿全部五个步骤：一份每周的供应商价格表，它应当触发一个审阅并预警的工作流。每周一，你的供应商会发来一份更新过的价格表。今天，有人挨个打开它们，用肉眼扫一遍，一旦有什么价格跳涨就去戳采购员一下。这正是那种应该自己运行的杂活。

## 第 1 步：挑一个有心跳的数据源

选那种按你能预测的时间表更新的数据。一份每周导出、一个每天早上刷新的公开页面、一个每周一收到报告的收件箱。心跳，正是让你能够把刷新自动化、而不用手动复制行的东西。如果数据源从不变化，你不需要工作流，你需要的是一次查询。省下这份力气。

对心跳要说得具体。不是笼统的“每周”，而是“每周一上午 9 点前通过邮件送达，每家供应商一个 CSV”。那份精确决定了你的触发器。一个落入收件箱的文件，暗示一个邮件触发器。一个会刷新的页面，暗示一次定时抓取。一个能主动呼出的系统，暗示一个 webhook。

**实战示例。** 供应商价格表每周一上午作为邮件附件送达。那是一个干净、可预测的心跳。触发器是“来自已知供应商、带价格表附件的新邮件”。没人需要记着去启动什么。

## 第 2 步：只在入口处规范化一次

原始数据源是杂乱的。列名会漂移，日期以三种格式进来，同一个实体以两种拼法出现，一家供应商叫它“unit price”，另一家叫它“price/ea”。把清洗放在一个地方，就在数据进入的入口处，这样下游每个步骤看到的都是单一而一致的形状。前端一个小小的规范化步骤，会多次回本。它之后的一切都变得更简单，因为它可以信任输入。

先确定规范形状，再把每个数据源映射上去。对于价格表，规范化后的一行也许是：supplier、sku、description、unit_price、currency、effective_date。无论某家供应商的表长什么样，规范化步骤输出的都是那个形状，别无其他。下游节点永远看不到原始的杂乱。

**实战示例。** 供应商 A 送来一个 Excel 文件，带一个以欧元计的“Cost”列。供应商 B 送来一个 CSV，带一个以美元计的“List Price”列。规范化步骤读取每一份，换算成统一货币，解析日期，并为每家供应商输出同样的六个干净字段。从这里开始，工作流既不知道也不关心一行数据来自哪家供应商。

## 第 3 步：在决策上分支，而不是在数据上分支

工作流的重点是一个决策。把那个决策显式地建模出来。如果某个值越过阈值，走一条路。否则，走另一条路。当各项彼此独立时，拆分列表并并行处理每一项。当两件事应当同时发生时，分叉成各自独立的路径。让分支保持可读。一张你整个团队一眼就能看懂的图，比一张只有作者才理解的巧妙图更有价值。

这里的陷阱是在原始数据上分支，而不是在决策上分支。你并不在乎价格是 12.40。你在乎的是它自上周以来的涨幅有没有超过你的容忍度。所以先算出决策，再在决策上分支。

**实战示例。** 对每一条规范化后的行，工作流查出上周同一 sku 的价格，算出百分比变化，然后分支：如果涨幅超过百分之五，走“标记它”那条路径；否则标记为已审阅并继续。因为每个 sku 彼此独立，列表被拆分，各行并行检查，所以一张上千行的表仍然一遍就能过完。

## 第 4 步：以一个动作收尾，并在关键处安排人工

最后一个节点应当做点什么：发出通知、更新那一行、开出工单、准备采购单。当动作有风险或不可逆时，先暂停等待批准。运行会等一个人签字确认，然后从它停下的地方原样接着跑。廉价、可逆的动作可以无人值守地运行。昂贵或单向的动作则要设一道人工闸门。

**实战示例。** 被标记的价格跳涨被汇集成一份摘要发给采购员：supplier、sku、旧价、新价、百分比变化。如果某个跳涨大到足以自动暂停一张已在途的订单，工作流会停在一个批准步骤，等采购员确认后才去动任何东西。那些常规的，就只发预警。

## 第 5 步：记录结果，让下一次运行更聪明

把结果写回去。一张工作流会读取并更新的表，成为一份共享记忆：它记得自己已经处理过什么，于是下一次运行会跳过重复项，只碰新的东西。它同时也是下周比较的数据来源。

**实战示例。** 每一条处理过的行都被写入一张以 supplier 和 sku 为键的 prices 表，并带上生效日期。那张表正是第 3 步用来计算“自上周以来的变化”的东西。工作流自己给自己供料。它也给了你一条干净的审计轨迹：哪些价格在何时变了，以及谁批准了对应的处理。

## 常见陷阱

- **没有真正的心跳。** 给一个很少变化的数据源做自动化，只是徒增运动部件而毫无回报。动手前先确认它的节奏。
- **在三个地方规范化。** 如果两个节点各自用各自的方式清洗数据，它们会漂移并互相矛盾。只在入口处规范化一次。
- **在原始值上分支。** 先算出决策，再在决策上分支。埋在五个不同节点里的阈值，日后根本改不动。
- **不可逆动作上没有人工闸门。** 在一次糟糕的解析上自动发出采购单，正是自动化落下坏名声的方式。给那些单向步骤设闸门。
- **忘了写回去。** 没有记忆，每一次运行都会把一切重新处理一遍，也无法检测出变化。这份记录不是可选项，它正是让循环运转起来的东西。
- **把文本当数字来比较。** 用一致的数值形状存储价格，并把它们当作数字来比较，这样从 9 涨到 100 才会读作上涨，而不是下跌。

这就是整套模式。一个有心跳的数据源、一个干净的入口、一个显式的决策、一个真实的动作，以及一份记忆。把这五样接在一起，数据集就不再是一个你去查看的文件。它变成了一个为你工作的系统。
`;

export default content;
