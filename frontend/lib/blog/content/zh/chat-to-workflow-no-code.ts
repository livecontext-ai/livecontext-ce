// chat-to-workflow-no-code - zh
const content = `你不需要写代码来构建一个 AI 自动化。你需要的是用大白话说出你想让什么发生。工具会把那句话变成一个你能看见、能运行、能修改的工作流。

这就是无代码 AI 自动化的全部承诺：描述任务，得到一个能运行的系统，并始终掌控它。

## 从结果出发，而不是从步骤

人们从老一代自动化工具带来的习惯，是先按步骤思考。用哪个触发器，哪个节点，哪个字段映射到哪个字段。在这里，这是本末倒置。

从结果出发。说出“完成”是什么样子。

“当一封支持邮件进来时，读它，判断它是 bug、账单问题还是一般咨询，用合适的语气拟一份回复，并把草稿放进一个供人工审阅的队列里。”

那一句话就足以开始。你描述了目标和工作的形状。工具会补上管道部分。

## 你得到的是一张图，而不是一个黑盒

当你描述任务时，工具会构建一张可读的图：一个触发器、几个步骤、它们之间的分支。你可以一眼看懂它。这比听上去更重要。

很多 AI 工具会把工作藏起来。你输入一个请求，某些事情发生了，然后你只能祈祷。一旦出错，你什么都没得检查。

在这里你看得见每一个节点。你看得见邮件从哪里进入，分类在哪里发生，账单问题走哪条分支，草稿在哪里被写出来，又在哪里等待人工。没有任何东西是隐含的。如果一个步骤存在，它就在画布上。

## 用聊天来打磨，或者动手来打磨

第一个版本很少是最终版本。打磨，正是无代码体现价值的地方。

你有两种方式来修改工作流，而且可以自由混用：

- **继续聊。** “另外，把任何提到退款的都标为紧急。”工具会加上那条分支并把它接好。
- **直接编辑节点。** 打开分类步骤，调整类别。打开草稿步骤，收紧语气。给某条分支改名。把某个步骤提前。

聊天对于结构性改动很快。直接编辑对于小微调很精确。二者互不排斥。图是唯一的事实来源，两条路径都写向同一张图。

## 每个步骤都是有范围的，这让它保持廉价

一个工作流并不是一个什么都干的大智能体。它是一组小步骤，而每个步骤只看它需要的东西。

分类步骤看到邮件正文并返回一个类别。它只需要这些，所以它只拿到这些。草稿步骤看到邮件和类别。审阅步骤看到草稿。

因为每个步骤拿到的是一小片上下文，而不是整段历史，所以 token 保持很小，成本保持很低。同一个任务的运行成本，大约是把所有东西交给一个什么都干的智能体、然后指望它别跑偏的十分之一。这份节省不是你手动设计出来的。它是把任务构建成一张有范围的图之后，自然而然掉下来的结果。

## 什么时候你仍然需要用到代码节点

无代码能覆盖大部分工作。它不必覆盖全部，假装它能，正是这类工具落下坏名声的地方。

当逻辑确实是机械而精确的时候，就动用代码节点：

- 把一个载荷重塑成另一个步骤期望的精确结构。
- 一个精确的计算、一条日期数学规则、一个不带模糊的阈值。
- 解析一种内置步骤不认识的格式。

这些正是几行代码比给模型一整段指令更清晰、更可靠的情形。重点不是回避代码。重点是不要在那些用描述能处理得更好的部分去写代码。用语言来做判断。用代码节点来做精确。

## 一个具体例子：支持收件箱分流

把这个支持例子从头到尾走一遍。

**触发器。** 一封新邮件落入支持收件箱。

**分类。** 一个有范围的智能体读取邮件并返回一个标签：bug、账单或一般咨询。它只看到邮件，别无其他。

**分支。** 图根据那个标签一分为三。这是一条你看得见的真实分支，而不是一个隐藏的决策。bug 走一条路，账单走另一条，一般咨询走第三条。

**拟稿。** 在每条分支上，一个步骤用合适的语气写一份回复。账单分支可以先拉取账户状态。bug 分支可以附上状态页的链接。

**审阅。** 每份草稿都落入一个队列。人来读它，需要就改，然后批准。没有那份批准，任何内容都到不了客户手里。

**审计。** 每一次运行都留下轨迹：什么进来了，得到了哪个标签，走了哪条分支，拟了什么，谁批准的。

你是通过描述把它构建出来的。你能读懂它，因为它是一张图。你能改动它，用聊天或用编辑。而当有人问某封邮件为什么得到了它得到的那份回复时，你能指出它走过的那条确切路径。

这才是无代码 AI 自动化应有的含义。不是一个你盲目信任的魔法盒子，而是一个你用文字描述、然后握在自己手里的系统。
`;

export default content;
