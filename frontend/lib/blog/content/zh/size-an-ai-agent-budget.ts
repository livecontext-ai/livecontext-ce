// size-an-ai-agent-budget - zh
// Translated from the English body; the structure must stay identical to it.
// Formulas and code samples are fenced on purpose: an inline code span over
// ~45 chars overflows the page on a phone. The hedges are load-bearing here.
const content = `一篇姊妹文章论证了：大多数 agent 预算不过是一串从未拒绝过任何一次调用的数字，并逐层拆解了执行机制：一个预算对象由什么构成，为什么运行中的上限最多只能拦住昂贵调用之后的那一次调用，以及每种技术栈实际上能强制执行什么。本文回答的是接下来的那个问题。你已经认同天花板应该是真实的。那么你该在框里填什么数字？

简短的答案是，你无法靠直觉挑出这个数字，因为你要约束的量是右偏的、随迭代次数超线性增长的，并且在不同步骤类型之间跨越三个数量级。冗长的答案就是本文的其余部分：一个你可以复现的生成模型、一个推导出来的安全系数、一个低于它金额上限根本无法被强制执行的下限，以及在你有资格引用某个尾部分位数之前所需的样本量。

**披露。** 下文描述的实现常量与预留机制来自 LiveContext 的 \`agent-service\`，也就是本博客所属的平台。请把它们读作某一个系统的选择（可在其社区版源码中核验），而不是经过调研的行业普遍实践。价格是示意性的目录快照；方法本身与价格无关。

## 为每一步设定可计算的预算

一个运行 \`n = k+1\` 次模型迭代、包含 \`k\` 次工具调用的步骤，其预期成本由固定提示词 \`P0\`、输入负载 \`I\`、每次工具结果返回的 tokens \`r\`、每轮输出 \`O\`，以及一个与 \`n(n-1)/2\` 成正比的累积项共同决定。下面每一行所依据的生成模型：

\`\`\`
prompt_i = (P0 + I) + (i-1) * (O_turn + r)      i = 1..n
\`\`\`

**表 4a：每步原型参数。** 这些是**人为构造的参数集合，不是实测的生产环境轨迹**，之所以公布，是为了让每一个派生列都能被复现。

| 步骤原型 | P0 + I | 每次工具结果的 r | 每轮 O | n |
|---|---|---|---|---|
| 分类 | 1,000 | n/a | 30 | 1 |
| 检索增强草稿 | 2,000 | 6,000 | 工具轮 60，最终 500 | 2 |
| 多工具研究 | 2,500 | 3,000 | 工具轮 80，最终 800 | 7 |
| 长文档摘要 | 120,300 | n/a | 1,500 | 1 |
| 浏览器步骤 | 1,800 | 8,000 | 动作轮 120，最终 250 | 13 |

**表 4b：每步预算测算。** 价格取自一份仓库目录快照，属于示意性质，并非供应商实时定价。方法本身与价格无关。最后一列使用一个固定的 S=3 作为示意系数；下一节会用一个推导出来的系数取而代之。

| 步骤原型 | 模型级别，标价（$/1M 输入、输出） | 输入 / 输出 tokens | 迭代次数 n | 预期成本 | 单次最大迭代（相对首次的倍数） | S=3（固定）下的预算 |
|---|---|---|---|---|---|---|
| 分类 | flash-lite 级别，0.25 / 1.50 | 1,000 / 30 | 1 | $0.00030 | 1.0x | $0.0009 |
| 检索增强草稿 | haiku 级别，1.00 / 5.00 | 10,060 / 560 | 2 | $0.01286 | 4.6x | $0.0386 |
| 多工具研究 | sonnet 级别，3.00 / 15.00 | 82,180 / 1,280 | 7（6 次工具调用） | $0.2657 | 8.6x | $0.797 |
| 长文档摘要 | flash 级别，0.30 / 2.50 | 120,300 / 1,500 | 1 | $0.0398 | 1.0x | $0.1195 |
| 浏览器步骤 | gpt-5.4 级别，2.50 / 15.00 | 656,760 / 1,690 | 13（12 次动作，8,000 token 快照） | $1.667 | 40x | $5.00 |

浏览器步骤上首次到末次 40x 的比值决定了整个设计：基于移动平均的预测，会把那次致命迭代低估一个数量级以上。这正是为什么预测需要一条完全忽略增长模式的最坏情况分支，姊妹文章对此有推导。

### 安全系数是推导出来的，不是猜出来的

\`\`\`
S = (n_q / n_p50) ^ alpha        where alpha = dlogC / dlogn

alpha in [1, ~2.3]: it approaches 1 for single-shot
steps, approaches 2 for accumulation-dominated steps,
and exceeds 2 when the first iteration is cheap
relative to the accumulated context.

alpha:  classify ~1.0 | long-doc ~1.0 | RAG draft 1.77
        multi-tool research 1.81 | browser 2.03
\`\`\`

如果某个步骤的 p99 使用的工具调用次数是其 p50 的两倍，那么在单次调用（single-shot）情形下它大约需要 S 为 2.0，但在工具密集的情形下则需要 3.4 到 4.1。凭感觉猜一个“2x”，会系统性地低估恰恰最需要余量的那些步骤。这些 alpha 是工作点处的切线值；读者若在观测到的 n 区间上测割线，会得到略大一些的数字。用精确模型做割线校验：研究步骤从 n 7 到 14 成本增长 3.66x（切线预测 2^1.81 = 3.51）；浏览器步骤从 n 13 到 26 成本增长 4.06x（切线预测 2^2.03 = 4.08）。

推论：**把允许的迭代次数翻一倍，金额天花板大致会变成四倍。**“我们把 max iterations 稍微调高一点吧”是一个 4x 的预算决定。

这也让迭代次数上限成为一个糟糕的金额上限。在平台默认的 100 次 max iterations 下，浏览器原型的天花板是 40,374,000 个提示词 tokens，即单个步骤 $101.11（相对预期的 $1.667），而研究原型的天花板是 15,496,000 tokens，即 $46.62（相对 $0.266）。作为计算出的数据点而非一个区间：7.7 倍于预期 n 时，浏览器步骤上仍留有 61x 的金额余量；14.3 倍时，研究步骤上留有 175x。

### 可执行性下限

由于按 agent 的预测需要两个样本，并且当单次迭代超过 \`budget/3\` 时会自我否决，粒度比必须满足：

\`\`\`
g = B_step / cost_of_one_iteration  >=  3
\`\`\`

因此每步预算的下限是最坏情况单次迭代的 3 倍。对照模型中不设上限的最坏情况单次迭代，这五个预算没有一个达标：分类 $0.0009 对下限 $1.04（g = 0.003），检索增强草稿 $0.0386 对 $1.56（g = 0.074），研究 $0.797 对 $4.68（g = 0.51），长文档 $0.1195 对 $1.39（g = 0.26），浏览器 $5.00 对 $8.76（g = 1.71）。

**表 5：可执行性下限**（示意性的目录价格与上下文窗口；请替换成你自己的）

| 模型级别 | 最坏情况单次迭代，上下文无上限 | 可执行的最小预算（3x） | 在 30K/2K 准入上限下的最坏情况单次迭代 | 上限下可执行的最小预算 |
|---|---|---|---|---|
| flash-lite | $0.348 | $1.04 | $0.0105 | $0.032 |
| haiku | $0.520 | $1.56 | $0.040 | $0.120 |
| flash | $0.464 | $1.39 | $0.014 | $0.042 |
| sonnet | $1.560 | $4.68 | $0.120 | $0.360 |
| gpt-5.4 | $2.920 | $8.76 | $0.105 | $0.315 |

任何低于“无上限”那一列下限的每步金额上限都是记账，不是执行。解法是**对输入施加准入上限**，而不是把预算调大：把准入的提示词限制在 30K tokens、把 \`max_tokens\` 限制在 2K，可以把下限压低 13 到 33 倍。

但准入上限会改变步骤自身的成本剖面，所以 \`B_step\` 必须在这些上限之下重新推导，而且这些上限首先得与该步骤本身相容：

- **研究步骤**：按原样即相容。它最大的一次迭代提示词约为 21K，低于 30K 的上限，因此预算保持不变，g 从 0.51 升到 6.6。
- **浏览器步骤**：大约在第 4 次迭代时突破 30K（每张快照增加 8,120 tokens）。裁剪到只保留最后三张快照，最大单次迭代降到约 26K，预期成本降到 $0.754，S=3 的预算降到 $2.26，g 升到 21.5。
- **长文档步骤**：30K 的提示词上限会直接拒绝它，因为它单次迭代就是 120K tokens。即便把准入的提示词限制在它自身的输入规模，g 仍只有 2.8，低于下限。它的 n 固定为 1，所以这里的控制手段是输入规模本身，而不是金额上限。

两种区制的规则：低于交叉点的步骤在构造上就是有界的（n 固定为 1 或 2，\`I\` 很小），应当通过限制输入来控制；只有高于交叉点的步骤，金额上限才真正起作用。**便宜的步骤限输入，昂贵的步骤限金额。**

关于这个实现中“默认关闭”到底指什么，有一点需要澄清，因为它很容易被理解反。对于任何目录行中带有上下文窗口和最大输出 tokens 的模型，最坏情况天花板是**始终开启**的：两个守卫都无条件地取 \`max(growth, lastDelta*2, worstCase)\`。默认关闭的是另一件事，即针对*缺少*这些元数据的模型的失败即**关闭**（fail-closed）行为（\`requireCtxWindow\`）。文档给出的理由是一个迁移窗口期：没有这些列的旧价格快照否则会拒绝每一次对话轮次。

### 分位数、样本量与复合式误杀

选择分位数，就是在选择误杀率。如果 \`B_step\` 是观测到的合法成本的 q 分位数，那么按构造，每步的误杀率恰好是 \`1-q\`。这是一个产品决策，不是统计决策。

在同时使用两者之前，先把它和安全系数调和好：上面的 S 公式，是当*成本*尾部不可测但 *n* 的尾部已知时你所使用的估计量。你为分位数挑选的 q，和你喂给 S 的 \`n_q\`，必须是同一个分位数。对于 n 固定的步骤，\`n_q / n_p50 = 1\`，S 退化为 1，因此分位数只能改从输入规模的方差中获得。

在你能够引用某个尾部分位数之前所需的样本量：\`1/sqrt(n(1-q))\` 是尾部*超出次数*的相对标准误，所以要把这个次数估到正负 30%，需要 \`n ~ 11/(1-q)\`。p90 大约需要 111 次运行，p95 约 220 次，p99 约 1,100 次，p99.5 约 2,200 次。把它们当作下界：分位数所对应的美元*数值*上的误差取决于尾部的密度，而对于右偏的成本分布，情况会明显更糟。低于大约 200 次运行，你无法诚实地声称自己有一个 p99，此时应改用结构性最坏情况来定预算。

每步的误杀会复合累积。若有 k 个步骤，各自以其自身的 p99 设上限，并假设各步成本相互独立、且每次运行都执行全部 k 个步骤，那么在某处触碰上限的运行占比是 \`1 - q^k\`：

\`\`\`
k = 3   ->  3.0%
k = 10  ->  9.6%
k = 20  -> 18.2%

A p95 per-step cap across 10 steps kills 40.1% of runs.

To hit a 1% run-level target:  q_step = (1 - target)^(1/k)
  k=3  -> p99.666 | k=10 -> p99.900 | k=50 -> p99.980
\`\`\`

各步之间的正相关（某一个过大的运行级输入同时抬高好几步）会拉低真实比率，所以把这些数值当作悲观的那一端。

按分布定预算，别按均值：每步成本是右偏的，均值大约落在 p70 附近，按它定预算会误杀大约 30% 的合法步骤执行，换算到运行层面就是 \`1 - 0.7^k\`。

每次步骤执行采集五个字段：提示词 tokens、补全 tokens、工具调用次数、模型 id、终止停止原因。其中四个正是一个迭代前守卫本来就要消费的数据，所以只要你能执行，你就能测量。

校准 n 有两个相互独立的锚点：Anthropic 自己的扩展规则（简单事实查找为 1 个 agent、3 到 10 次工具调用；直接比较为 2 到 4 个子 agent、每个 10 到 15 次调用；复杂研究则超过 10 个子 agent），以及一条平均的 GitHub issue 修复轨迹，在 40 步之后达到 48.4K tokens 的上下文峰值，整条轨迹累计约 1.0M tokens（[arXiv 2509.23586](https://arxiv.org/html/2509.23586v1)）。以此对照，被广泛提及的 25 个 superstep 的默认值（至今仍是 langgraph-sdk schema 的默认值，在 ReAct 循环中大致相当于 12 次工具调用）会扼杀真实工作，而 200 步的上限则形同虚设。

**操作流程：**

1. 采集带有那五个字段的步骤执行记录。
2. 从你的运行级目标反推所需的每步分位数：\`q_step = (1 - target)^(1/k)\`。
3. 检查你的样本是否支撑得起它：你至少需要 \`11/(1-q_step)\` 次运行，在 k=10 且运行级目标为 1% 时，大约是 11,000 次。**如果支撑不起，就到此为止，改用有界的结构性最坏情况（表 5）来定预算。** 只把你*能*估出来的分位数用于发现结构性上限是否松得离谱，而不要用它来设定上限。这才是常见情形，装作不是这样，正是一个按 p95 设定的上限最终误杀 40% 运行的由来。
4. 如果样本确实支撑得起，就通过把 \`log(cost)\` 对 \`log(n)\` 做回归来测量 alpha，并把 \`B_step\` 设在 \`q_step\` 上。
5. 对照**有界的**最坏情况单次迭代检查 \`g >= 3\`。如果不达标，请添加准入上限，而不是抬高预算，并在这些上限之下重新推导 \`B_step\`。
6. 设定运行级上限（见下一节）。
7. 刻意给某一步喂过量输入，确认停止原因确实被触发。

## 运行级上限、扇出，以及为什么把各步上限相加是错的

正确的运行级界限是在最坏情况路径上对节点**执行次数**求和，而不是对节点求和：

\`\`\`
B_run = max over execution paths P of  sum over nodes v in P of  m_v * B_v

m_v = M inside a split of width M
    = L for a loop body
    = 1 otherwise

Exclusive branches contribute max, not sum.
\`\`\`

把每步上限相加在三个方面都是错的，而且这些偏差指向相反的方向：

1. **它在扇出子图上恰好少算了 M 倍。** 一条相加为 $0.837 的 3 节点流水线，当最后两个节点位于一个宽度为 50 的 split 内部时，真实最坏情况是 $41.78。
2. **它对永远不可能同时执行的互斥分支多算了。**
3. **它在统计上不可达。** 对 10 个满足 \`p99/p50 = 3\` 的独立对数正态步骤、各自以其中位数的 3 倍设上限，各上限之和大约是运行总额真实 p99 的 1.88 倍。把这个倍数当作方向性参考：真实的步骤成本部分相关，这会缩小差距。一个基本不可能触发的上限，同样也抓不住一次中等程度的结构性故障。

**汇聚效应。** 一个相互独立的扇出所需的相对余量*更小*，小 \`sqrt(M)\` 倍：

\`\`\`
S_run = 1 + (S_step - 1) / sqrt(M)

S_step = 3:  M=5 -> 1.89 | M=10 -> 1.63 | M=50 -> 1.28 | M=200 -> 1.14
\`\`\`

实例演算：研究步骤（均值 $0.2657）的 50 个分支，若天真地按 \`M * B_step\` 定为 $39.85，而汇聚后的上限是 $17.00，在同等风险下把运行级上限收紧了 2.3 倍。请把独立性假设大声说出来：如果分支成本由某个运行级属性驱动（例如一个过大的输入被扇出到每个分支），那么这些成本就是完全相关的，节省将彻底消失。

每步上限和每次运行上限承担着不同的职责，这正是决定它们大小的因素。步骤上限是经过调优的，预期会偶尔触发，触发时截断某一步的输出。运行级上限是一个断路器，应当几乎从不触发，每一次触发都是一起需要调查的事故（M 爆炸了、某个循环重新进入了扇出、某个输入是正常值的 100 倍）。

**扇出需要的是准入控制，而不是中途拦截。** 以运行中检查方式实施的运行级上限，会在分支飞行途中把它们杀掉，产生一组不确定的部分结果集：50 个分支各 $0.836，在 $10 的运行上限下 50 个中完成 11 个；在 $20 下 50 个中完成 23 个；至于哪些胜出，取决于启动顺序。在派生之前就预留完整的 \`M * b\`，会把这变成“拒绝运行”，那是明确且可重试的。

预留机制，如 \`BudgetReservationService\` 中所实现：

- 在派生时，子任务请求的额度会在同一个事务内，对调用链上的**每一个祖先**原子地预留。第一次拒绝会抛出异常，从而让事务回滚掉此前每一个祖先的更新。不存在手工补偿逻辑。
- 由此买到的不变式：A 的所有后代的 \`consumed\` 之和，在任意深度上都保持在 A 的预算之内，且热路径上无需运行时遍历整棵树。
- 每一个针对祖先的预留都是一条带条件的单条 SQL UPDATE，没有先 SELECT 再 UPDATE，因此不存在 TOCTOU。只有当该祖先的可用预算覆盖得住请求时，它才会递增 reserved 列：

  \`\`\`
  free = credit_budget - credits_consumed - credits_reserved
  UPDATE ... SET credits_reserved = credits_reserved + :req
   WHERE id = :ancestor
     AND (credit_budget IS NULL OR free >= :req)
  \`\`\`

  是否成功由返回的行数是否为 1 决定。一个无限额的祖先会以一次空操作写入命中，同样返回 1。
- 预留额度的确定：显式请求优先（负值被拒绝）；否则默认取**所有祖先中最小的可用预算**，若每个祖先都无限额则取零。
- 结算在子任务终止时沿同一条链走一遍，对每个祖先在单次更新中释放所持预留并记入实际成本，在同一事务中以相同的增量写入 \`consumed\` 和 \`consumed_from_subagents\`，使不变式在构造上成立。预留相关的列在 ORM 层被标记为不可更新，因此一次脏刷新无法悄悄改写它们。
- 泄漏的预留在**启动时**清扫，而不是靠超时：一个无状态 worker 不可能拥有任何早于它自身的预留，因此启动时存在的每一笔非零持有预留在定义上都是孤儿，会在一次更新中被清除。该清扫绝不能让启动失败。
- 调用链本身存放在凭据映射的一个保留键上，最近的在前，对于根调用则不存在（因此级联在根处是空操作），并由派生方 agent 为每个子任务在前面追加一项。

有独立证据表明：让上限成为真上限的是锁，而不是那个数字。在一篇编录了 63 起事故的 2026 年预印本中所报告的一项受控实验里（[arXiv 2606.04056](https://arxiv.org/abs/2606.04056)），一个存在竞态的 asyncio Python 预算计数器 30 次中有 30 次超出上限，而一个加锁正确的 Python 计数器和一个 Rust 仿射类型预算各自 30 次中有 0 次超出。

由此推出的父级预算约束：要派生 M 个各持上限 b 的子任务，父级在派生时刻需要至少 \`M * b\` 的**可用**预算，而不是预期总额。对研究原型，在 M=50 且 b=$0.797 时，即便这次运行实际大约只花 $13.3，父级也需要 $39.85 的可用额度。若按预期支出给父级定额，就只有 \`1/S\` 的分支能拿到资金：在 S=3 时，$13.29 的可用预算除以 $0.797 的预留，只够资助 50 次派生中的 16 次，拒绝 34 次。

## 读日志：从症状定位到出错的维度

**表 6：从症状定位到出错的预算维度**

| 你看到的现象 | 哪个维度出了问题 | 确认信号 | 在何处讨论 |
|---|---|---|---|
| 隐形击杀：完成结果看起来正常，但输出系统性地更短 | 终止响应 / 可观测性 | 停止原因分布显示存在部分停止，而持久化的状态却写着 COMPLETED | 姊妹文章，冲击发生的时刻 |
| 太紧：预算在第 2 或第 3 次迭代就叫停 | 定额分位数 / 安全系数 | 被杀运行的 token 数落在 p50 附近，输入看起来很普通 | 安全系数是推导出来的 |
| 装饰性：在很长的时间窗口内零次预算叫停 | 上限量级 | 观测窗口内没有任何拒绝；观测到的最大运行成本远低于上限 | 姊妹文章，开篇的测试 |
| 超支 / 上限过晚：实际成本在尾部以一个稳定的、与模型规模相当的量超过上限 | 执行点 / 预测 | 恰恰在步骤最昂贵的地方最严重 | 姊妹文章，关于一次迭代的滞后 |
| 作用域绑定错误：拒绝几乎 100% 发生在粗粒度层级 | 作用域 / 守卫顺序 | 每步上限从不生效 | 姊妹文章，预算对象的五个组成部分 |
| 扇出饥饿：派生了 N 个分支，实际执行少于 N 次 | 父级定额 / 预留策略 | 拒绝被归因于父级的预留，而非子任务的预算 | 运行级上限与扇出 |
| 预留泄漏：在相同输入和相同 M 下，拒绝数逐日上升 | 预留生命周期 | 持有的预留从未结算 | 运行级上限与扇出 |
| 单位错误：迭代次数相同，成本却相差一个数量级 | 单位 | 每次迭代的平均成本在各原型间跨越约 430 倍（分类 $0.00030 对浏览器 $0.128） | 为每一步设定可计算的预算 |

配置了预算不等于强制执行了预算，而且有已发布的证据。LiteLLM 对通过其 API 动态添加的模型接受了 \`max_budget\` 和 \`budget_duration\`，持久化了这些值，却从不执行；而同样的配置写在启动文件里却是有效的（[issue #25799](https://github.com/BerriAI/litellm/issues/25799)，已由后续 PR 关闭）。一个同类缺陷则涉及预算在其周期到期后从不重置（[#25495](https://github.com/BerriAI/litellm/issues/25495)）。请在测试中断言执行行为。不要因为某个字段存在就假定它生效。
`;

export default content;
