// cap-ai-agent-cost-budgets - zh
const content = `大多数 AI 成本的恐怖故事都有一个共同的根源：一个没有上限的智能体。它循环，它重试，它拖着一大团上下文到处走，而在账单出现之前没人发现。解药不是一个更聪明的模型。它是给每个智能体一份硬预算，在花费发生之前就强制执行，而不是在之后。

## 为什么无上限的智能体是一种财务风险

一个自主智能体自行决定它的下一步。那是它的特性，同时也是它的敞口。三种失败模式会把一个正常的任务变成一个开着的水龙头。

**循环。** 智能体试了某个办法，没成，它试一个变体，然后一直试下去。没有上限，它会为一个够不着的目标烧掉一次次调用。

**重试。** 一个不稳定的工具或一次限流触发重试。重试会叠加。看着像一次调用的东西，会变成二十次，每一次都付全额的上下文成本。

**长上下文。** 每次模型调用都会重新发送到目前为止的整段对话。一个不断累积历史的任务，在每一步上都比前一步付得更多。一次长运行里的最后一次调用，成本可能是第一次的许多倍。

这些都不罕见。它们正是一个被给了目标却没有上限的系统的正常行为。一份预算，把那份开放式的风险变成一个已知的、封顶的数字。

## 一份按智能体计的预算应当封顶什么

一份预算只有在触及上限时真能停下工作，才是有用的。它应当为那些真正驱动成本和运行时间的东西封顶：

- **总花费。** 一个以额度或 token 计的硬上限。当智能体触及它时，它停下。不超支，不搞“就再来一点点”。
- **模型调用次数。** 直接给循环封顶。一个发不出第二十一次调用的智能体，无法永远循环下去。
- **工具调用。** 有些工具要花钱或会触及外部配额。给智能体能动用它们的次数封顶。
- **挂钟时间。** 一个卡住的智能体不该跑上一个小时。给它设超时。

让预算名副其实的那条规则是：当上限被触及时，智能体停下，并由工作流来处理它。它不会悄悄接着跑，也不会静默失败。它停下，而运行会记录下它是因为触及预算才停下的。

## 给智能体能触及的工具和数据划定范围

预算是答案的一半。范围是另一半，而且它在需要任何上限之前就降低了成本。

一个能看见一切的智能体，会试图去用一切。把整个数据库给它，它就在整个数据库上推理，而你为那些 token 付费。只把这一步所需要的工具和数据给它，它就在构造上保持很小。

对一个分类步骤来说，那意味着消息正文和一个用来返回标签的工具。别无其他。对一个草稿步骤来说，是消息和类别。一个范围收得很紧的智能体，在每一次调用上都更便宜，因为它的上下文很小；而它也更安全，因为它无法游荡进不属于它职责的数据或动作里。

范围收窄了爆炸半径。预算给剩下的封顶。两者你都想要。

## 按智能体和按运行分别设预算

一个数字不够。你需要在两个层级设预算。

**按智能体。** 每个步骤都有它自己的、按其职责裁剪的上限。一次快速分类应当有一份极小的预算。一个要读几份文档的研究步骤则得到更多。把每个智能体裁剪到它实际的工作量，意味着某个贪吃的步骤无法花掉全部额度。

**按运行。** 整个工作流也有一个上限。即便每个单独的智能体都待在它自己的预算之内，一次扇出成数百条并行分支的运行也能积少成多。一个运行级的上限防的是总和，而不只是各个部分。

两者合在一起，给你一个可预测的封套：每一步一个已知的最坏情况，整次运行一个已知的最坏情况。那正是把“AI 成本”从一个意外，变成一个你能围着它做规划的成本项的东西。

## 按智能体和按工具监控花费

预算阻止失控的成本。监控告诉你成本实际上住在哪里，好让你去调优它。

以细颗粒度追踪花费：

- **按智能体。** 哪一步花费最多？往往是某个节点做了超出它所需的事，背着太多上下文，或者用了一个比任务所需更大的模型。
- **按工具。** 哪些工具调用占了大头？一个在每一项上都被调用的昂贵外部 API，会悄悄成为账单的主体。
- **按运行。** 一次典型的运行花多少，一次糟糕的运行又花多少？两者之间的差距，正是你的循环和重试藏身的地方。

有了这个视角，你就能有的放矢地调优。削减某一步的上下文。在质量允许的地方把它降到一个更便宜的模型。加一道去重护栏，让同一项不会被工具调用两次。每一次改动都是可度量的，因为你能看着那个数字动起来。

## 把它拼起来

失控的 AI 成本是一个设计问题，而不是一个建模问题。你从结构上解决它。

把每个智能体的范围划定到它职责所需的工具和数据。给每个智能体一份它不能超出的硬预算。给整次运行封一个上限。按智能体和按工具盯住花费，并在钱实际去向的地方调优。

做到这些，成本就不再是那个拦着你交付的东西。它变成一个你有意设定、自动强制执行、并能逐行辩护的数字。
`;

export default content;
