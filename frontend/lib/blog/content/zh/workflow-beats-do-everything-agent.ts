// workflow-beats-do-everything-agent - zh
const content = `单个自主智能体的演示总是很唬人。你给它一个目标，它思考，它调用工具，它带着一个答案回来。然后你把它放进生产环境，账单来了，结果开始飘忽，而没人能告诉你它当初为什么那么做。

问题不在模型。问题在形状。一个什么都干的智能体，对大多数真实工作来说是错误的形状。

## 成本：上下文就是计价表

每次智能体调用模型，它都会重新发送它的上下文。那些指令、那段历史、到目前为止的每一个工具结果。一个什么都干的智能体把这一切累积在一段长长的对话里，而上下文随着每一步不断增长。

你在每一次调用上都为那份上下文付费。一个十步的任务，成本不是十次小调用。它是十次调用，每一次都背着一堆越来越大的、此前发生过的一切。

一个工作流把任务拆成有范围的步骤，并只给每个步骤喂它需要的东西。分类步骤看到消息。草稿步骤看到消息和类别。发送步骤看到批准过的草稿。没有哪个步骤拖着整段历史。

给每个智能体喂一小片，而不是整份对话记录，token 数就会陡降。在实践中，同一个任务的运行成本大约便宜十倍。这不是什么把戏。它是不为重新发送某个步骤根本用不到的上下文付费的直接结果。

## 控制：确定性分支 对上 即兴发挥

一个什么都干的智能体在运行时自行决定它的路径。有时它走对的那条。有时它发明一条新的。你是在信任一个概率性系统每次都做出同样的路由选择，而它不会。

一个工作流把路由做成显式的。一个账单问题走下账单分支，是因为图这么规定，而不是因为模型这一次心血来潮。那些模糊的判断（这是账单还是 bug？）仍然发生在某个步骤内部。而结构性决策（一个账单项接下来会怎样）是固定的。

那道分割正是全部要点。让模型去做只有模型能做的事，也就是阅读与判断。别让它去即兴那些你需要可靠的部分。

## 可审计性：一条你能指出来的路径

当一个智能体在单一循环里做完一切时，记录就是一堵由推理和工具调用砌成的墙。重建究竟发生了什么，简直像考古。

一个工作流给你一次你能读懂的运行。这是输入。这是它走的那条分支。这是每个步骤收到了什么、返回了什么。这是每个步骤的成本。这是发送前谁批准的。当有人问某个客户为什么得到了某一份回复时，你从轨迹里回答，而不是靠猜。

## 调试：一个有边界的表面

一个大智能体失败时，给你的是一整个巨大的失败任你盯着看。是计划出了错，是某个工具结果不好，还是二十轮之前某条指令丢了？你无法把它隔离出来，因为一切共享同一段上下文。

一个工作流是在某个节点上失败。草稿步骤产出的语气不对，那你就打开草稿步骤。它的输入就在那儿。你改动那个步骤，重跑，其余的原封不动。小、有边界、可重复，正是正常软件调试的方式。

## 说句公道话：什么时候单个智能体才是对的选择

有范围的工作流并不总是答案，假装它总是答案，本身就是另一种炒作。

在以下情况下，动用一个单独的自主智能体：

- **任务确实是开放式的。** 探索性研究，或者调试，此时下一步完全取决于上一步的结果。你无法预先画出分支，因为它们还不存在。
- **路径又短又便宜。** 一次性的查询或一份快速草稿不需要一张图。图反倒成了额外开销。
- **你还在摸索它的形状。** 早期，就让一个智能体到处跑，看它实际上都做了什么。那种行为里稳定的部分，正是你日后要提炼进工作流的东西。

诚实的规则是：如果你能把步骤画出来，就构建一个工作流。如果你目前确实还画不出来，那么就现在而言，智能体才是对的工具。

## 混合式：工作流负责编排，智能体负责模糊部分

最好的生产系统不是二选一。它们是一个内部含有智能体的工作流。

工作流拥有结构：触发器、分支、汇合、批准、重试、每个步骤上的预算。在确定性要紧的地方，它是确定性的。

在单个节点内部，智能体去做那些需要判断的活：给这条消息分类、拟这份回复、抽取这些字段、总结这份文档。这些智能体每一个都是有范围的。它拿到一个清晰的输入、一小组工具、一份它不能超出的预算，然后向后续步骤返回一个清晰的输出。

你既拿到了有范围步骤的成本画像，又拿到了显式分支的可靠性，还在推理真正有用的地方拿到了模型的推理。智能体处理那个模糊的子任务。工作流处理其余的一切，而且它作为一个你能运行、能监控、能交给别人的应用交付出去。

先问问你的工作里，哪些部分真正需要判断。把那些部分包进有范围的智能体里。把其余的接成一张图。那就是能经受住与生产环境正面接触的形状。
`;

export default content;
