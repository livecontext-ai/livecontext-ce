// workflow-beats-do-everything-agent - zh
// Translated from the English body; keep the structure identical to it
// (10 h2, 6 tables, 5 fenced formula blocks, 21 source links). The formulas are
// fenced on purpose: an inline formula over ~45 chars overflows the page on a phone.
const content = `## 我删掉的那个数字

本文的早期版本说过，一条限定范围（scoped）的工作流比一个"什么都干"的智能体"大约便宜十倍"。那个数字背后没有推导、没有假设、也没有来源，所以它已经被删掉了。

也没有任何已发表的来源可以拿来替换它。没有任何厂商基准测试、论文或轨迹记录，把同一项工作分别实现为一条限定范围的流水线和一个自主智能体，并对两边的成本与成功率都做了插桩测量。这一类里最经典的文章，Anthropic 的 [Building Effective Agents](https://www.anthropic.com/engineering/building-effective-agents)，一个成本数字都没有；它对这个主题的全部论述只有两句话："智能体系统常常以延迟和成本换取更好的任务表现，你应当考虑这种取舍在什么时候是划算的"，以及"智能体的自主性意味着更高的成本，以及误差累积的可能性"。第二句话正好断言了本文的论点，却没有附上任何数字。

流传的那些倍数彼此并不可互换。"3-10x"流传的是关于 LLM 调用次数的说法，"5-30x"流传的是关于每项任务 token 数的说法，两者都没有可追溯的一手来源。唯一一个把假设摆在明面上的数字，[某篇 dev.to 文章给出的 12.4x](https://dev.to/awxglobal/why-your-llm-agent-costs-10x-more-than-your-estimate-4o78)，是这样构造出来的：一个 800 token 的系统提示词在每次调用时重发、每个请求 4 轮、3.5 次工具调用且每次开销 250 token，而基线只计入用户自己的提示词和模型的回复。因此它的 12.4x 是在固定轮数下关于"提示词与工具开销比例"的陈述，而不是关于一整项工作的陈述；改变轮数，倍数就会变。这是该类文章里唯一一个你能审计的推导，而审计的结果表明：它衡量的并不是另外两个区间所衡量的东西。那些确实拿两种形态做对比的框架类文章（[Sashido](https://www.sashido.io/en/blog/agentic-workflows-roi-without-expensive-agents)、[LindleyLabs](https://lindleylabs.com/blog/agent-or-pipeline-a-decision-framework-for-ai-engineers)、[Retool](https://retool.com/resources/ai-workflows-vs-agents)）印出来的是公式和决策树，变量都没有填值。

这个领域里确实存在一个真实且有出处的 10x，但它并不是我删掉的那个说法：Anthropic 的[缓存读取乘数恰好是基础输入价格的 0.1x](https://platform.claude.com/docs/en/about-claude/pricing)，所以一个被缓存的输入 token 精确地比未缓存的便宜十倍。这只适用于输入 token 中被缓存的前缀部分，仅此而已。

本文余下部分的规则：这里印出来的每一个比值，都在页面上从写明的假设推导出来。没有一个是引用来的。

## 钱花在哪里：两个成本函数，其中一个是二次的

先讲机制，这样后面每个数字都能靠检查来证伪。

Messages API 是无状态的。因此智能体的第 \`i\` 次调用携带 \`In_i = B + (i-1)g\`，其中 \`B = S + T + P0\` 是系统提示词、工具定义与初始载荷，\`g = a + r\` 是每轮的增长（助手输出加上被追加回去的工具结果）。对 N 次调用求和：

\`\`\`
I(N) = N*B + g*N*(N-1)/2
\`\`\`

第一项是前缀税，随 N 线性增长。第二项是累积税，随 N 二次增长。在第 \`i\` 轮产生的一个 token，会被当作输入再读 \`(N - i)\` 次。

限定范围工作流的成本是：

\`\`\`
C_wf = sum over k of [ p_in^k*(s_k + t_k + d_k) + p_out^k*o_k ]
\`\`\`

它对 K 是线性的，因为第 k 步只收到它声明的输入 \`d_k\`，绝不会收到第 1 步到第 k-1 步的对话记录。注意价格上的 \`^k\`：工作流可以把每一步路由到不同的模型而不付出任何代价。单循环智能体只要在对话中途切换模型，就要为其累积前缀付一次完整的缓存重写，所以实际中它会把整个循环钉死在一个模型上。要在智能体架构内部做逐次调用的路由，就需要一个子智能体边界，而这本身就是一个划定范围的决策，并且每个子智能体都要付一份全新的前缀成本。

分解带来的上界是精确的，而不是修辞性的。把一个 N 次调用的循环切成 K 个限定范围的分段，会把累积项恰好除以 K，因为 \`K * g*(N/K)^2/2 = g*N^2/(2K)\`。三步拆分把累积项上的节省上限锁在 3x。任何声称三步工作流带来 10x 的说法，其来源是前缀税、载荷宽度或模型路由，而不是打破了那个二次项。

工具定义是 B 中一个实打实的组成部分。Anthropic 报告称，一套五个 MCP 服务器的配置（GitHub、Slack、Sentry、Grafana、Splunk）在模型开始干任何活之前就[消耗大约 55,000 token 的定义](https://platform.claude.com/docs/en/agents-and-tools/tool-use/tool-search-tool)。按标价算，这在 Opus 4.8 上是每次未缓存调用 $0.275，而且这个数字在本文末尾描述的分词器变更边界上保持 token 数不变，所以应把它当作下限而非估计值。

## 完整算例：客服工单分诊，每一条假设都写出来

任务：给一张工单分类、查账户、检索知识库、起草回复、审校。

| 参数 | 符号 | 智能体取值 | 工作流取值 | 出处 |
|---|---|---|---|---|
| 系统提示词 | S | 1,500 tok | 每步 250-600 tok | 假设：一个包打天下的提示词对四个窄提示词 |
| 工具定义 | T | 6 个工具，900 tok | 每步 30 tok，合计 120 tok | 假设；没有厂商公布过单个工具的数字 |
| 初始载荷 | P0 | 600 tok | 600 tok（工单正文） | 两边是同一张工单 |
| 前缀 | B = S+T+P0 | 3,000 tok | 不适用（按步计） | 以上各项之和 |
| 每轮增长 | g = a + r | 1,000 tok | 0（不携带对话记录） | a=300 输出，r=700 工具结果 |
| 调用数 / 步骤数 | N / K | 8 次调用 | 4 个 LLM 步骤 + 2 个确定性步骤 | 关于这项工作需要什么的判断 |
| 价格 | p_in / p_out | $3.00 / $15.00 每 MTok | 相同 | [Sonnet 4.6 标价](https://platform.claude.com/docs/en/about-claude/pricing)，于 2026-07-22 核实 |

上面每一个 token 数都是写明的假设，不是测量值。没有一个来自 token 计数接口。

**智能体账本，N=8。** 输入每次调用恰好增长 g = 1,000，所以这张表是一个从 3,000 到 10,000 的等差数列；只有两个端点和第二行有信息量。

| 调用 | 输入 token | 累计输入 | 输出 token | 累计成本 |
|---|---|---|---|---|
| 1 | 3,000 | 3,000 | 300 | $0.0135 |
| 2 | 4,000 | 7,000 | 300 | $0.0300 |
| ... | 每次 +1,000 | | 300 | |
| 8 | 10,000 | 52,000 | 300 | $0.1920 |

52,000 这个总数与闭式解吻合：\`8*3,000 + 1,000*(8*7/2) = 24,000 + 28,000\`。成本：52,000 x $3/MTok = $0.156 输入，加上 2,400 x $15/MTok = $0.036 输出。**每张工单 $0.192。**

**工作流账本，同一项工作。** 前缀列与载荷列被拆开列出，因为下面两个杠杆正是从这个拆分中来的。

| 步骤 | 用 LLM？ | 模型 | 系统提示词 | 工具定义 | 声明的数据 | 输入 | 输出 | 单步成本 |
|---|---|---|---|---|---|---|---|---|
| 分类 | 是 | Sonnet 4.6 | 250 | 30 | 600 | 880 | 60 | $0.00354 |
| 账户查询 | 否 | （确定性） | 0 | 0 | 0 | 0 | 0 | $0 |
| 知识库检索 | 否 | （确定性） | 0 | 0 | 0 | 0 | 0 | $0 |
| 构造知识库查询 | 是 | Sonnet 4.6 | 250 | 30 | 70 | 350 | 25 | $0.00143 |
| 起草 | 是 | Sonnet 4.6 | 600 | 30 | 1,810 | 2,440 | 400 | $0.01332 |
| 审校 | 是 | Sonnet 4.6 | 600 | 30 | 450 | 1,080 | 80 | $0.00444 |
| **合计** | | | **1,700** | **120** | **2,930** | **4,750** | **565** | **$0.02273** |

起草步骤声明的数据是：工单 600 + 标签 60 + 账户事实 250 + 知识库前三条摘录 900 = 1,810。

这份账本只给模型 token 定价。在托管产品上，一个终态工作流节点还要收固定 1 个积分（$0.001），这 6 个节点合计增加 $0.006，把工作流推到 $0.0287。下文所有比值都是仅计 token 的比较；含托管成本的版本会在它第一次真正产生影响的地方给出。

**这套配置的头条数字：8.4x**（$0.192 / $0.02273），若计入托管的按节点收费则是 6.7x。单看输入 token 的比值是 10.9x（52,000 / 4,750）。输出 token 的比值只有 4.2x（2,400 / 565），正是它把混合后的数字拖到了 10x 以下：两种形态都必须真的写出同一封约 400 token 的回复。

有两个杠杆在缓存之后依然存在，但被缓存削弱了。**前缀税**：在未缓存情况下，智能体把它 3,000 token 的包打天下前缀重发八次（24,000 token），对上工作流总共 1,820 token 的系统提示词加工具定义，是 13.2x。开启增量缓存后，智能体的前缀部分降到 \`1.25B + 0.1(N-1)B\` = 5,850 有效 token，这个杠杆降到 3.2x。**载荷宽度**，按快照口径：在最后一次调用时，智能体的输入包含 7,600 token 的累积载荷与工具对话记录（初始 600 加上 7 x 1,000 的增长），对上工作流最宽的那一步，其声明输入是 1,810 token，即 4.2x。一旦开启缓存，这个比较的计价口径就变了，因为智能体较早的增量是按 0.1x 重读的：按累计有效 token 计，智能体的载荷增长要花 \`1.25*1,000*7 + 0.1*1,000*21\` = 10,850 token，对上工作流的 2,930 个声明数据 token，是 3.7x。两者背后的机制是同一个：声明的输入是对原始观测的一次投影。

## 这个比值是 N 的函数，而 N 就是全部论点所在

在本例的假设下，智能体成本为 \`0.0015N^2 + 0.012N\` 美元。在 N=8 处校验：$0.096 + $0.096 = $0.192。

| N（智能体调用数） | 智能体成本 | 工作流成本 | 比值 | 这个 N 代表什么 |
|---|---|---|---|---|
| 2 | $0.030 | $0.0227 | 1.3x | 短路："垃圾信息，升级处理" |
| 4 | $0.072 | $0.0227 | 3.2x | 最少量的工具使用，没有重试 |
| 6 | $0.126 | $0.0227 | 5.5x | 有一次查询被重试 |
| 8 | $0.192 | $0.0227 | 8.4x | 本文的算例 |
| 12 | $0.360 | $0.0227 | 15.8x | 智能体在知识库里探索 |
| 20 | $0.840 | $0.0227 | 37.0x | 兜圈子，或者确实是一张很难的工单 |

解 \`0.0015N^2 + 0.012N = 0.022725R\`：比值在 N = 8.94 次调用时等于 10，在 N = 3.84 次调用时等于 3。不给出 N 就报一个成本比值，是没有意义的。

这张表有一个公平性约束：高 N 的那几行只有在这项工作确实需要那么多次调用时才成立。一个智能体用 20 次调用去做工作流 4 步就做完的事，那是在兜圈子，这属于能力层面的结论，必须作为能力结论来论证，而不能偷偷塞进一张成本表里。

## 先把智能体的缓存做对，再做比较

已发表的"工作流对智能体"比值，通常拿来对比的是一个未缓存的智能体。差距的大部分都消失在缓存里，所以在比较之前先给它定价。

Anthropic 的[缓存乘数](https://platform.claude.com/docs/en/build-with-claude/prompt-caching)是精确的：5 分钟缓存写入 = 基础输入的 1.25x，1 小时写入 = 2x，缓存读取 = 0.1x。盈亏平衡点也是公开的：5 分钟缓存在一次读取之后就回本（1.25 + 0.1 = 1.35x，对上未缓存的 2x）；1 小时缓存需要两次读取（2 + 0.2 = 2.2x，对上 3x）。

开启增量多轮缓存后，智能体的有效输入变成：

\`\`\`
1.25B + 0.1(N-1)B + 0.1g(N-2)(N-1)/2 + 1.25g(N-1)
\`\`\`

二次项系数从 \`p_in*g/2\` 降到 \`0.1*p_in*g/2\`，正好是在工作流原本占优的那一项上打了精确的 10 倍折扣。

在 N=8 时，每次调用的有效输入依次为 3,750 / 1,550 / 1,650 / 1,750 / 1,850 / 1,950 / 2,050 / 2,150 = 16,700 token，对上未缓存的 52,000。这是 $0.0501 输入加 $0.036 输出 = **$0.0861**。缓存把智能体成本削减了 55%，并把头条数字从 8.4x 压到 **3.8x**。

注意开启缓存后是什么在主导成本：每轮增量的 1.25x 写入，\`1.25 * 1,000 * 7 = 8,750\`，占 16,700 有效 token 的 52%。工作流残存的优势是前缀税和载荷宽度，而不是重发带来的二次项。

缓存把差距压平，但没有把它抹平。在 N=20 时，缓存后的智能体花 $0.241，对上未缓存的 $0.840，仍然是工作流的 10.6 倍，因为 19 次按 1.25x 计价的缓存写入加上 20 轮生成内容是不可压缩的。

在这里，工作流几乎捕捉不到同样的收益。Sonnet 4.6 上可缓存前缀的最小长度是 1,024 token（于 2026-07-22 对照缓存文档核实；在 Fable 5 和 Mythos 5 上是 512，在 Opus 4.7 上是 2,048，在 Opus 4.6、Opus 4.5 和 Haiku 4.5 上是 4,096）。这里每个工作流步骤的稳定前缀就是它的系统提示词加工具定义，280 到 630 token，在上述每一个模型上都低于阈值。低于最小长度的前缀会静默失败：不会返回任何错误，而 \`cache_creation_input_tokens\` 和 \`cache_read_input_tokens\` 都读作 0。注意，把某一步路由到 Haiku 4.5 会把它的阈值抬到 4,096，所以下文那个路由后的配置离可缓存更远，而不是更近。

可操作的修法有一个公开的盈亏平衡点。把某个高频步骤的前缀整合到它所运行模型的可缓存最小长度之上，并把断点放在其后，这样该步骤的每一次运行都按 0.1x 读取。在 5 分钟 TTL 下，这从第二个请求起就开始回本，而且[缓存读取会免费刷新 TTL](https://platform.claude.com/docs/en/build-with-claude/prompt-caching)，所以一个至少每五分钟被命中一次的步骤可以按写入价无限期保持热态。

有一件事缓存做不到：被缓存的 token [仍然占用上下文窗口](https://platform.claude.com/docs/en/build-with-claude/context-windows)。它改变的是你为这些 token 付多少钱，而不是它们算不算数。它救不了任何人脱离上下文耗尽或上下文腐化。

## 四格矩阵，以及真正的 10x 到底藏在哪里

把分类、构造知识库查询和审校路由到 Haiku 4.5（$1/$5），只把起草留给 Sonnet 4.6，工作流降到每张工单 $0.0165（分类 $0.00118 + 知识库查询 $0.000475 + 起草 $0.01332 + 审校 $0.00148）。

| | 工作流，同一模型（$0.0227） | 工作流，路由后（$0.0165） |
|---|---|---|
| **智能体，未缓存（$0.192）** | 8.4x | 11.7x |
| **智能体，已缓存（$0.0861）** | 3.8x | 5.2x |

我的默认选择是把右上格反过来用：给智能体做缓存，给工作流做路由，预期 5.2x。在大约 N=4 以下我根本不会去搭这条工作流，因为比值不到 3x，搭建成本收不回来（见结尾一节）；在大约 N=12 以上，二次项会替你做决定。

单循环智能体必须在每次调用上都跑它钉死的那一个模型。一个 Opus 4.8 智能体（$5/$25）并不是把同样的 token 数做等量替换，因为 Opus 4.7 及之后的模型使用了新的分词器，对同样的文本会多产生大约 30% 的 token。套用这个上浮：约 67,600 输入和 3,120 输出，即 $0.338 + $0.078 = $0.416，对上路由后工作流的 $0.0165，是 25.3x。这是一个关于路由的论证，不是关于上下文窗口的论证。

仅凭上下文纪律能得出的笼统结论，推导如下：在智能体已缓存、两种形态使用同一模型的前提下，比值在 N=6 时是 2.8x，N=8 时是 3.8x，N=12 时是 5.8x。所以在一个合理的 N 区间内大致是 3x 到 6x，任何高于此的部分都是一个缓存或路由决策，必须作为这样的决策明确说出来。

厂商的价格结构让路由变得可预测。Anthropic 当前的每一个模型，输出价格都恰好是输入的 5 倍（Opus 4.8 $5/$25，Sonnet 4.6 $3/$15，Haiku 4.5 $1/$5）。OpenAI 当前的每一个模型，输出价格是输入的 6 倍（gpt-5.6-sol $5.00/$30.00，gpt-5.4 $2.50/$15.00，gpt-5.4-mini $0.75/$4.50），唯一的例外是 gpt-5.4-nano 的 6.25 倍（$0.20/$1.25）。[DeepSeek](https://api-docs.deepseek.com/quick_start/pricing) 的输出价格恰好是缓存未命中输入的 2 倍（deepseek-v4-flash $0.14/$0.28，deepseek-v4-pro $0.435/$0.87）。在同一家厂商内部，输入与输出的配比对成本结构的影响，比选哪个模型更大。另外，批处理档位是第五个、且只有工作流才用得上的杠杆，适用于对延迟不敏感的步骤：在 Anthropic 和 OpenAI 上对输入和输出统一打五折，在 Gemini 上是标准价的一半，在 Anthropic 上还能与缓存乘数叠加。

## 这个模型在哪些地方失效

| 条件 | 对比值的影响 | 在此处的量级 | 为什么会这样 |
|---|---|---|---|
| 短任务（N<4） | 坍缩，甚至可能反转 | N=2 时 1.3x | 智能体会短路；工作流总是跑完它固定的路径 |
| 输出主导型任务 | 趋近于 1 | N=8 时一份 5,000 词报告为 2.2x | 两种形态都要写出同样的交付物 |
| 大块共享上下文 | 可能反转 | 一份 50k 文档上 5D 对 1.95D | 除非先缓存该文档，工作流会逐步重发 |
| 可并行的广度优先研究 | 有利于多智能体 | 某厂商评测上 +90.2% | 自主性买来的是流水线无法穷举的覆盖面 |
| 工具搜索（延迟加载） | 削弱前缀优势 | 厂商声称定义削减 >85% | 智能体无需重构架构即可拿到前缀节省 |
| 工具数 > 30-50 | 有利于工作流，理由是正确性 | 未定价 | 工具选择准确率下降 |
| 与模型相关的工具附加开销 | 改变 B | 各模型间 290 到 804 tok | 在任何 schema 之前就有的固定系统提示词成本 |
| 服务端工具计费 | 在 token 模型之外 | 每 1,000 次网络搜索 $10 | 按调用计费，不是按 token |
| 分词器变更 / 价格回归 | 使带日期的数字失效 | 约多 30% token；$2/$10 变 $3/$15 | Opus 4.7 起使用新分词器；Sonnet 5 的引导期定价于 2026 年 8 月 31 日结束 |

其中有四条值得展开。

**输出主导型任务。** 一份 5,000 词的报告大约是 6,700 个输出 token，两边都按 $15/MTok 计为 $0.1005。保持本例的输入不变（智能体 $0.156，工作流 $0.01425），比值是 $0.2565 / $0.1145 = 2.2x，并且随着交付物变大而继续下降。

**大块共享上下文**是反转情形。如果每一步都需要同一份 50k token 的文档，一条五步工作流会把它重发五次（5D），而一个缓存后的八次调用智能体只付 1.25D + 7 x 0.1D = 1.95D。工作流要想赢，只有把该文档放在最前面、放在步骤专属指令之前，并对它做缓存（1.25D + 4 x 0.1D = 1.65D）。

**可并行的研究是针对本文整个论点最有力的已发表反例。** Anthropic 报告称，一个多智能体系统（一个 Claude Opus 4 主控编排若干 Claude Sonnet 4 子智能体）在其内部研究评测上[比单智能体 Claude Opus 4 高出 90.2%](https://www.anthropic.com/engineering/multi-agent-research-system)，并在同一篇文章中指出：智能体使用的 token 大约是一次聊天交互的 4 倍，而多智能体系统大约是 15 倍。这是自主性以很大的成本倍数换来了很大的质量提升，同时它也是一个智能体架构在跨子智能体边界做逐步的模型路由。Anthropic 自己给出的前提条件才是诚实的表述：只有当任务价值能覆盖这个倍数时才划算，而当所有智能体都需要同样的上下文、或工作中存在大量依赖关系时，它并不合适。

**工具搜索**是专门针对前缀税这一论点的最强反驳。Anthropic 表示，延迟加载工具[通常能把工具定义占用的上下文削减 85% 以上](https://platform.claude.com/docs/en/agents-and-tools/tool-use/tool-search-tool)，只加载所需的 3-5 个工具，这让一个包打天下的智能体无需重构架构就能拿到大部分前缀节省。这是一条没有公开方法论的厂商声明，应当按厂商声明来看待。Anthropic 自己给出的触发条件是：工具数达到 10 个或以上，或定义超过 10k token 时使用工具搜索。同一页面还写道，一旦可用工具超过 30-50 个，工具选择的准确率就会下降，这给"划定范围"这一论点提供了一条完全不依赖 token 算术的可靠性支撑。

## 每次成功运行的成本，以及让论点反转的条件

真正重要的比较是 \`C/q\`：成本除以各自形态的成功率。工作流 20% 的重跑率会把它的成本乘以 1.2，把 8.4x 的头条数字削到 7.0x。而一个在上下文内自行恢复的智能体，付出的是多出来的几轮、按二次方定价的调用。

重复运行的一致性崩塌得比头条准确率更快。在最初 2024 年的 [tau-bench retail](https://arxiv.org/abs/2406.12045) 论文中，当时最好的函数调用智能体一致性差到 pass^8 跌破 25%。生产环境意味着把同一项工作跑很多次，所以正确的指标是 pass^k 而不是 pass@1，真正撑得住的是这个结构性论点，而不是 2024 年的任何绝对数值。

成功率还会随任务长度衰减，其方式使得缩小范围在可靠性上的收益是超线性的。[Toby Ord 的半衰期模型](https://www.tobyord.com/writing/half-life)预测：80% 成功率的时间跨度约为 50% 跨度的 1/3，90% 约为 1/7，99% 约为 1/70；作者明确说明该模型是拟合到单一任务集上的，其普适性未知。[METR 的测量](https://arxiv.org/abs/2503.14499)显示 80% 时间跨度大约比 50% 跨度短 5 倍，比半衰期模型的 3 倍更陡，所以这两者是给该效应划出一个区间，而不是互相印证。而且这种失败是结构性的，不只是分数低一点：对 3,100 多条轨迹的 [HORIZON 研究](https://arxiv.org/html/2604.11978v1)把 72.5% 的失败归因于过程层面的原因（环境错误、指令错误、规划错误、历史累积），并报告了从部分稳健到近乎系统性失败的一次突变式转变。该研究同时主张，仅靠分解并不是解药：它呼吁的是分层规划与执行期验证，而不仅仅是把任务拆开。

最强的对立模型来自 [Zartis](https://www.zartis.com/ai-agent-cost-optimisation-why-token-cost-is-the-wrong-number-to-optimise/)：

\`\`\`
total_cost_per_task = (token_cost + infrastructure_cost) / reliability_rate
                      + failure_rate * human_remediation_cost
\`\`\`

他们的算例表明，一旦可靠性从 70% 提升到 95%，一个每次调用贵 5 倍的架构（$0.05 对 $0.01）整体上反而便宜 5.7 倍（约 $8,835/天 对 约 $50,100/天）。他们的 token 数、小时费率和补救分钟数都是那篇文章写明的假设，不是测量值，而且他们对比的两个架构差别在于上下文宽度而非自主性。但这个论证的结构依然成立。

用这里的数字解一遍。工作流 $0.0227，缓存后的智能体 $0.0861，每张工单差额 $0.0634。如果一次失败在人工补救上要花 \`H\`，且智能体的成功率比工作流高 \`dq\`，那么当 \`dq * H > 0.0634\` 时智能体胜出。按 $100/小时的分析师、每次补救 10 分钟计，H = $16.67，于是 0.38 个百分点的成功率优势就够了。按 5 分钟和 $80/小时计，H = $6.67，阈值是 0.95 个百分点。把话说白：**在任何非平凡的人工补救率下，token 比值都不再是起决定作用的那一项。** 这个盈亏平衡所基于的 3.8x，在一个百分点的成功率差异面前只是舍入误差，甚至未缓存的 8.4x 也只需要 1.02 个百分点的优势就会翻转（差额 $0.1693 对 H = $16.67）。这把刀是双刃的，而这正是为什么支持划定范围的可靠性论证（更短的任务跨度、更少的工具、经过验证的步骤间契约）比本文刚刚推导出来的成本论证更重要。

多花钱也买不到准确率。在 [GAIA](https://hal.cs.princeton.edu/gaia) 上，一个使用 o3 Medium 的智能体花了 $2,828.54 只拿到 28.48% 的准确率，而 Gemini 2.0 Flash 花 $7.80 拿到 32.73%。在同一个[评测项目](https://arxiv.org/abs/2510.11977)中，在所测试的 36 个模型-基准组合里，提高推理强度在多数情况下反而降低了准确率。

## N 是结果，不是输入

以上一切都把 N 当作参数。在生产中它是涌现出来的，这也正是这个比值有长尾的原因。

这条尾巴不是渐进爬升，而是一个阶跃函数，这正是它难以防守的地方。有一起生产事故留下了这种形状的记录：一次运行连续若干轮平稳地保持在每轮约 70k 提示词 token 和 700 完成 token，便宜到基于均值的预测一直在批准下一轮，然后某一轮突然暴涨到约 2M token。移动平均恰恰会稀释掉这种情况。

能抓住它的那个界限根本不做平均：

\`\`\`
projectedNext       = max(growth projection, last delta x 2, worstCaseSingleIter)
worstCaseSingleIter = cost(full context window, full max output)
\`\`\`

第二项对增长模式是不变量，这正是全部要点所在。按一行 Opus 级别的参数定价，200k 上下文、64k 最大输出、$15/$75 每 MTok，一次最坏情况的迭代是 200,000 x $15/MTok + 64,000 x $75/MTok = $3.00 + $4.80 = $7.80。该模型的单次迭代完全可能比一个小额账户余额还贵，所以守卫在平稳期的第一轮就触发，而不是把宝押在均值上。

成本估算出于同样的理由，宁可估贵不估便宜：没有价格行的模型会回退到 $15/$75 每 MTok，即快照中最高的档位，因为早先一个接近于零的回退值曾经悄悄地把预算守卫整个绕开了。

在能计算每次成功的成本之前，必须先对一次运行的结束方式做分类。某个生产环境的分类法恰好列出 3 个终态类别下的 10 个停止原因：成功（COMPLETED）；部分完成（MAX_ITERATIONS、TIMEOUT、BUDGET_EXHAUSTED、LOOP_DETECTED、STOPPED_BY_USER），定义为"干净地终止，但没有按计划完成任务，输出可用但被截断或提前结束"；以及失败（CANCELLED、NO_TOOLS、ERROR、INACTIVITY_TIMEOUT）。TIMEOUT 和 INACTIVITY_TIMEOUT 被刻意划入不同类别：超出挂钟时间预算属于部分完成，而在看门狗窗口内没有产出任何 token、思考、工具调用或工具结果则属于失败。

确定性步骤这个锚点让比较变得具体。一个终态工作流节点，无论完成还是失败，在托管产品上都收固定 1 个积分（$0.001）；只有被跳过的节点是免费的，自托管版本会为可观测性记录同样的 1 积分账目行，但永远不扣减，因为余额是无限的。按 Sonnet 4.6 的标价 $3/MTok，一个积分相当于 333 个标价输入 token；在托管产品上，1.8x 的云端 LLM 加价使其约为 185 个 token，所以任何超过大约 186 token 的提示词，成本都高于一整个确定性步骤。在节点面板的大约 60 种节点类型中，只有 4 种（Agent、Classify、Guardrail、Browser Agent）会真正调用 LLM。

## 如何用你自己的数字跑一遍

1. **测量 token。** 上面算例中的每一个数都是假设。用提供方的 token 计数接口，针对真实的工单文本、真实的工具 schema 和真实的知识库摘录，把它们替换掉。
2. **从已有的轨迹中测出 N，不要去估。** 比值大致与 N 成二次关系，所以 N 估错，在头条数字上就是平方级的误差。
3. **在引用任何"每次成功成本"数字之前，先按停止原因和终态类别把一个月内终止的运行分类。** 部分完成与失败两种结束方式的补救成本不同，而三个类别中只有一个算作一次成功的运行。

这个模型不包含两样东西，也不应从中推断出它们。它对输出质量只字未提：它给 token 定价，而其中任何数字背后都没有成功率的测量。它也忽略了工程成本，而这一项在实践中决定了这类判断中的大多数。我自己的估计，和这里其他一切一样，作为假设写明：一条带有声明式步骤间契约的五步工作流，搭建大约需要三个工程师日，维护每月约半天，而把一个智能体接上六个工具大约要半天。按上文补救所用的同样 $100/小时计，这大约是前期多 $2,000、经常性每月多 $400。对上缓存智能体每张工单 $0.0634 的差额，仅前期这一部分就需要大约 31,500 张工单才能回本，维护部分还额外需要每月大约 6,300 张工单。低于这个量，你所处的那一行盈亏平衡表写的就是：去搭智能体。
`;

export default content;
