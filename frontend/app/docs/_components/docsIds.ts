// Id of the article container in the docs layout. Shared by the layout (which
// renders it and points the skip link at it) and the mobile menu (which moves
// focus to it after a link is followed), so the two can never drift apart.
export const DOCS_ARTICLE_ID = 'docs-article';
