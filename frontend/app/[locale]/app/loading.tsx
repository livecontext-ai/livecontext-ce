/**
 * Loading state for /app route
 *
 * Returns null to avoid double skeleton effect - each page handles its own loading state.
 * The NavigationLoader progress bar at the top of the page provides navigation feedback.
 */
export default function AppLoading() {
  return null;
}
