/**
 * Loading state for conversation page
 *
 * Returns null to avoid double skeleton effect - the page handles its own loading state.
 * The NavigationLoader progress bar at the top provides navigation feedback.
 */
export default function ConversationLoading() {
  return null;
}
