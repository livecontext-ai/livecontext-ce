'use client';

/**
 * Application view page (publication-based).
 * Content is rendered by the parent layout.tsx which uses ApplicationDetailView.
 */
export default function ApplicationPage() {
  return null;
}
